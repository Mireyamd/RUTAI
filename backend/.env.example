# Configuración de RUTAI API
APP_NAME=RUTAI API
APP_VERSION=0.1.0
ENVIRONMENT=development

# CORS: "*" para permitir todo en desarrollo, o lista separada por comas.
# Ejemplo: CORS_ORIGINS=http://localhost:5173,http://localhost:3000
CORS_ORIGINS=*

# Supabase (aún no se usa, preparado para sprints futuros)
SUPABASE_URL=
SUPABASE_KEY=
